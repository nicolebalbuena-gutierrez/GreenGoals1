## GreenGoals environment variables
## Copy to `.env` for local dev (never commit `.env`)

# Server
PORT=3000
JWT_SECRET=replace-with-a-long-random-string

# Optional: used for AI challenge generation / chat moderation
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# Optional: used for sustainability news endpoint
NEWS_API_KEY=YOUR_NEWSAPI_KEY_HERE

# Firebase Admin SDK (server-side)
# Provide credentials via environment OR use an ignored local json file.
# See your existing firebase setup docs in the repo.
